version: '2'

services:
  tygercaddy:
    image: morph1904/tygercaddy:alpine-testing
    environment:
      - username=admin
      - password=secret
      - email=admin@example.com
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
